import { DynamicTableUngDirective } from './dynamic-table-ung.directive';

describe('DynamicTableUngDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTableUngDirective(null);
    expect(directive).toBeTruthy();
  });
});
